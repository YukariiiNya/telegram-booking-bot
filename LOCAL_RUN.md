# Локальный запуск бота для демонстрации

## Быстрый старт

### 1. Установите зависимости

```bash
pip install -r requirements.txt
```

### 2. Настройте .env файл

Скопируйте `.env.local` в `.env` и укажите ваш токен бота:

```bash
copy .env.local .env
```

Откройте `.env` и замените `your_bot_token_here` на реальный токен от @BotFather.

### 3. Запустите бота

```bash
python main_local.py
```

Бот запустится в режиме polling (без webhook) и будет работать локально.

## Доступные команды

После запуска откройте вашего бота в Telegram и попробуйте:

- `/start` - Начать работу и зарегистрироваться
- `/bookings` - Посмотреть историю записей
- `/contact` - Написать сообщение в поддержку
- `/help` - Справка

## Функции бота

### ✅ Регистрация
- Бот запрашивает номер телефона при первом запуске
- Номер используется для связи записей из Bukza с пользователем Telegram

### 📅 История записей
- Команда `/bookings` показывает все записи пользователя
- Записи разделены по статусам: активные, завершённые, отменённые
- Для завершённых записей показывается оценка

### 💬 Связь с поддержкой
- Команда `/contact` позволяет отправить сообщение
- Сообщение пересылается в канал поддержки (если настроен)

### 🔔 Автоматические уведомления
- При создании записи в Bukza
- Напоминание за 24 часа до визита
- Запрос отзыва после посещения

### ⭐ Сбор отзывов
- После посещения бот просит оценить услугу от 1 до 5
- При оценке 5 предлагает оставить отзыв на 2ГИС и Яндекс.Картах

## Тестирование без Bukza

Для демонстрации без реального Bukza можно:

1. Зарегистрироваться через `/start`
2. Вручную добавить тестовую запись в базу данных
3. Протестировать команды `/bookings`, `/contact`, `/help`

## Остановка бота

Нажмите `Ctrl+C` в терминале для остановки.

## Переключение на production

Для запуска на сервере с webhook используйте `main.py` вместо `main_local.py`.
