# Быстрый старт - Пошаговая инструкция

## 📋 Что нужно сделать (по порядку)

```
1. Создать Telegram бота
   ↓
2. Создать поддомен
   ↓
3. Развернуть бота на сервере
   ↓
4. Настроить вебхуки в Bukza
   ↓
5. Протестировать
```

---

## 1️⃣ Создание Telegram бота (5 минут)

### Шаги:
1. Откройте Telegram
2. Найдите [@BotFather](https://t.me/botfather)
3. Отправьте команду: `/newbot`
4. Придумайте имя: `Мой Салон Бот`
5. Придумайте username: `mysalon_booking_bot`
6. **Скопируйте токен** (выглядит так: `1234567890:ABCdefGHIjklMNOpqrsTUVwxyz`)

✅ **Готово!** Сохраните токен - он понадобится позже.

---

## 2️⃣ Создание поддомена (10 минут)

### Если ваш сайт: `mycompany.ru`

1. Зайдите в панель управления доменом (Reg.ru, Timeweb и т.д.)
2. Найдите раздел "DNS" или "DNS-записи"
3. Добавьте A-запись:
   - **Имя:** `bot`
   - **Тип:** `A`
   - **IP:** IP вашего сервера
4. Сохраните

**Результат:** `bot.mycompany.ru` → ваш сервер

📖 **Подробная инструкция:** [SUBDOMAIN_SETUP.md](SUBDOMAIN_SETUP.md)

---

## 3️⃣ Развертывание бота (30-60 минут)

### ⭐ Вариант A: Railway.app (РЕКОМЕНДУЕТСЯ - БЕСПЛАТНО!)

**Преимущества:**
- ✅ Бесплатный домен (типа `your-bot.up.railway.app`)
- ✅ $5 кредитов каждый месяц
- ✅ Автоматический SSL
- ✅ Деплой за 5 минут

📖 **Подробная инструкция:** [RAILWAY_DEPLOY.md](RAILWAY_DEPLOY.md)

**Кратко:**
1. Загрузите код на GitHub
2. Зарегистрируйтесь на [railway.app](https://railway.app)
3. Создайте проект из GitHub
4. Добавьте PostgreSQL
5. Настройте переменные окружения
6. Готово! Railway даст вам бесплатный домен

---

### Вариант B: VPS сервер

```bash
# 1. Подключитесь к серверу
ssh root@your-server-ip

# 2. Установите зависимости
apt update && apt upgrade -y
apt install -y python3.11 python3.11-venv postgresql nginx certbot python3-certbot-nginx git

# 3. Клонируйте проект
git clone <your-repo> /opt/telegram-bot
cd /opt/telegram-bot

# 4. Создайте виртуальное окружение
python3.11 -m venv venv
source venv/bin/activate
pip install -r requirements.txt

# 5. Настройте PostgreSQL
sudo -u postgres psql
CREATE DATABASE booking_bot;
CREATE USER botuser WITH PASSWORD 'secure_password';
GRANT ALL PRIVILEGES ON DATABASE booking_bot TO botuser;
\q

# 6. Создайте .env файл
nano .env
```

Заполните `.env`:
```env
BOT_TOKEN=ваш_токен_от_botfather
DATABASE_URL=postgresql+asyncpg://botuser:secure_password@localhost:5432/booking_bot
BUKZA_API_URL=https://api.bukza.com
BUKZA_API_KEY=ваш_ключ_bukza
WEBHOOK_HOST=https://bot.mycompany.ru
WEBHOOK_PATH=/webhook/bukza
LINK_2GIS=https://2gis.ru/ваш_салон
LINK_YANDEX_MAPS=https://yandex.ru/maps/ваш_салон
```

```bash
# 7. Настройте Nginx
sudo nano /etc/nginx/sites-available/telegram-bot
```

Вставьте:
```nginx
server {
    listen 80;
    server_name bot.mycompany.ru;

    location / {
        proxy_pass http://127.0.0.1:8080;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}
```

```bash
# 8. Активируйте конфиг
sudo ln -s /etc/nginx/sites-available/telegram-bot /etc/nginx/sites-enabled/
sudo nginx -t
sudo systemctl restart nginx

# 9. Получите SSL
sudo certbot --nginx -d bot.mycompany.ru

# 10. Создайте systemd сервис
sudo nano /etc/systemd/system/telegram-bot.service
```

Вставьте:
```ini
[Unit]
Description=Telegram Booking Bot
After=network.target

[Service]
Type=simple
User=root
WorkingDirectory=/opt/telegram-bot
Environment="PATH=/opt/telegram-bot/venv/bin"
ExecStart=/opt/telegram-bot/venv/bin/python main.py
Restart=always

[Install]
WantedBy=multi-user.target
```

```bash
# 11. Запустите бота
sudo systemctl daemon-reload
sudo systemctl enable telegram-bot
sudo systemctl start telegram-bot

# 12. Проверьте статус
sudo systemctl status telegram-bot
```

### Вариант B: Railway.app (проще)

📖 **Полная инструкция:** [RAILWAY_DEPLOY.md](RAILWAY_DEPLOY.md)

**Кратко:**
1. Зарегистрируйтесь на [railway.app](https://railway.app)
2. Создайте новый проект из GitHub
3. Добавьте PostgreSQL из маркетплейса
4. Настройте переменные окружения
5. Сгенерируйте домен
6. Railway автоматически развернет бота

**Ваш бесплатный домен:**
```
https://your-bot-production.up.railway.app
```

Используйте этот URL в настройках вебхуков Bukza!

📖 **Подробная инструкция:** [DEPLOYMENT.md](DEPLOYMENT.md)

---

## 4️⃣ Настройка вебхуков Bukza (10 минут)

### В админ-панели Bukza создайте 2 вебхука:

### Вебхук 1: Новая запись

**Название:** `Telegram - Новая запись`

**URL:**
```
https://bot.mycompany.ru/webhook/bukza?message=newrega&phone=%2B7%20%28{phone}%29
```

**Метод:** `POST`

**Тело запроса:**
```json
{
  "code": "{code}",
  "name": "{name}",
  "start": "{start}",
  "end": "{end}",
  "resource": "{resource}"
}
```

### Вебхук 2: Отмена записи

**Название:** `Telegram - Отмена`

**URL:**
```
https://bot.mycompany.ru/webhook/bukza?message=cancel&phone=%2B7%20%28{phone}%29
```

**Метод:** `POST`

**Тело запроса:** (такое же)

📖 **Подробная инструкция:** [WEBHOOK_CHEATSHEET.md](WEBHOOK_CHEATSHEET.md)

---

## 5️⃣ Тестирование (5 минут)

### Шаг 1: Проверьте бота в Telegram

1. Найдите вашего бота в Telegram
2. Отправьте `/start`
3. Поделитесь номером телефона
4. Должно прийти подтверждение регистрации

### Шаг 2: Создайте тестовую запись в Bukza

1. Зайдите в Bukza
2. Создайте запись на тестовый номер телефона
3. Проверьте, что клиент получил уведомление в Telegram

### Шаг 3: Проверьте логи

```bash
# На VPS
sudo journalctl -u telegram-bot -f

# На Railway/Heroku
# Смотрите в веб-интерфейсе
```

---

## ✅ Чеклист готовности

- [ ] Telegram бот создан, токен получен
- [ ] Поддомен создан и работает (`ping bot.mycompany.ru`)
- [ ] Бот развернут на сервере
- [ ] SSL-сертификат установлен (`https://bot.mycompany.ru`)
- [ ] PostgreSQL настроена
- [ ] `.env` файл заполнен
- [ ] Бот запущен и работает
- [ ] Вебхуки настроены в Bukza
- [ ] Тестовая запись прошла успешно
- [ ] Клиент получил уведомление

---

## 🆘 Проблемы?

### Бот не отвечает в Telegram
```bash
# Проверьте статус
sudo systemctl status telegram-bot

# Проверьте логи
sudo journalctl -u telegram-bot -n 50
```

### Вебхук не работает
```bash
# Проверьте доступность
curl https://bot.mycompany.ru/webhook/bukza

# Должен вернуть 400 (это нормально без данных)
```

### SSL не работает
```bash
# Проверьте сертификат
sudo certbot certificates

# Обновите сертификат
sudo certbot renew
```

---

## 📚 Дополнительные материалы

- [SUBDOMAIN_SETUP.md](SUBDOMAIN_SETUP.md) - Создание поддомена
- [DEPLOYMENT.md](DEPLOYMENT.md) - Развертывание на разных платформах
- [BUKZA_SETUP.md](BUKZA_SETUP.md) - Настройка Bukza
- [WEBHOOK_CHEATSHEET.md](WEBHOOK_CHEATSHEET.md) - Шпаргалка по вебхукам
- [ARCHITECTURE.md](ARCHITECTURE.md) - Архитектура системы

---

## 💡 Советы

1. **Начните с Railway** - проще всего для первого раза
2. **Сохраните все пароли** - токен бота, пароль БД, API ключи
3. **Тестируйте на своем номере** - перед запуском для клиентов
4. **Настройте мониторинг** - чтобы знать, если бот упал
5. **Делайте бэкапы БД** - раз в день

---

## 🎉 Готово!

Теперь ваши клиенты будут получать:
- ✅ Подтверждение записи
- ⏰ Напоминание за 24 часа
- ⭐ Запрос обратной связи
- 📍 Ссылки на отзывы (при оценке 5)
