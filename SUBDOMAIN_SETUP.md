# Создание поддомена для Telegram-бота

## Что такое поддомен?

Если ваш сайт: `mycompany.ru`  
Поддомен будет: `bot.mycompany.ru` или `telegram.mycompany.ru`

Это бесплатно, если у вас уже есть основной домен!

---

## Где создавать поддомен?

Поддомен создается **там же, где вы купили основной домен** или **где управляете DNS**.

### Популярные регистраторы доменов в России:

1. **Reg.ru** - [reg.ru](https://www.reg.ru)
2. **Timeweb** - [timeweb.com](https://timeweb.com)
3. **Beget** - [beget.com](https://beget.com)
4. **Nic.ru** - [nic.ru](https://www.nic.ru)
5. **Selectel** - [selectel.ru](https://selectel.ru)

---

## Инструкция для Reg.ru

### Шаг 1: Войдите в личный кабинет
1. Перейдите на [reg.ru](https://www.reg.ru)
2. Нажмите "Войти" (правый верхний угол)
3. Введите логин и пароль

### Шаг 2: Выберите домен
1. В меню слева выберите "Домены"
2. Найдите ваш домен (например, `mycompany.ru`)
3. Нажмите на него

### Шаг 3: Перейдите в DNS-настройки
1. Найдите раздел "Управление DNS"
2. Нажмите "Редактировать зону"

### Шаг 4: Добавьте A-запись
1. Нажмите "Добавить запись"
2. Заполните поля:
   - **Тип записи:** `A`
   - **Поддомен:** `bot` (или `telegram`, `api`)
   - **IP-адрес:** IP вашего сервера (например, `185.123.45.67`)
   - **TTL:** `3600` (или оставьте по умолчанию)
3. Нажмите "Добавить" или "Сохранить"

### Результат:
Теперь `bot.mycompany.ru` будет указывать на ваш сервер!

---

## Инструкция для Timeweb

### Шаг 1: Войдите в панель управления
1. Перейдите на [timeweb.com](https://timeweb.com)
2. Войдите в личный кабинет

### Шаг 2: Выберите домен
1. В меню выберите "Домены"
2. Нажмите на ваш домен

### Шаг 3: DNS-записи
1. Перейдите на вкладку "DNS-записи"
2. Нажмите "Добавить запись"

### Шаг 4: Создайте A-запись
Заполните:
- **Тип:** `A`
- **Имя:** `bot`
- **Значение:** IP вашего сервера
- **TTL:** `3600`

Нажмите "Добавить"

---

## Инструкция для Beget

### Шаг 1: Войдите в панель
1. Перейдите на [beget.com](https://beget.com)
2. Войдите в панель управления

### Шаг 2: Домены и поддомены
1. В меню выберите "Домены и поддомены"
2. Найдите ваш домен

### Шаг 3: Добавьте поддомен
1. Нажмите "Добавить поддомен"
2. Введите имя: `bot`
3. Укажите IP сервера
4. Сохраните

---

## Инструкция для Cloudflare (если используете)

### Шаг 1: Войдите в Cloudflare
1. Перейдите на [cloudflare.com](https://cloudflare.com)
2. Войдите в аккаунт

### Шаг 2: Выберите домен
1. Выберите ваш домен из списка

### Шаг 3: DNS настройки
1. Перейдите на вкладку "DNS"
2. Нажмите "Add record"

### Шаг 4: Создайте запись
- **Type:** `A`
- **Name:** `bot`
- **IPv4 address:** IP вашего сервера
- **Proxy status:** 🟠 DNS only (отключите прокси для начала)
- **TTL:** Auto

Нажмите "Save"

---

## Как узнать IP сервера?

### Если у вас VPS:
IP указан в письме от хостинга или в панели управления VPS.

### Если используете Railway/Heroku:
Вам НЕ нужна A-запись! Используйте CNAME:
- **Тип:** `CNAME`
- **Имя:** `bot`
- **Значение:** `your-app.up.railway.app` (или `.herokuapp.com`)

### Если бот на том же сервере, что и сайт:
Используйте тот же IP, что и у основного сайта.

Узнать IP:
```bash
ping mycompany.ru
```

---

## Проверка поддомена

### Через 5-30 минут после создания проверьте:

**Способ 1: Ping**
```bash
ping bot.mycompany.ru
```

Должен показать ваш IP.

**Способ 2: nslookup**
```bash
nslookup bot.mycompany.ru
```

**Способ 3: Онлайн-сервисы**
- [whatsmydns.net](https://www.whatsmydns.net)
- Введите `bot.mycompany.ru`
- Проверьте, что DNS распространился

---

## Настройка SSL-сертификата

После создания поддомена нужен SSL (HTTPS).

### На сервере с Nginx:

```bash
# Установите certbot (если еще не установлен)
sudo apt install certbot python3-certbot-nginx

# Получите сертификат для поддомена
sudo certbot --nginx -d bot.mycompany.ru

# Certbot автоматически настроит Nginx
```

### Проверка SSL:
```bash
curl -I https://bot.mycompany.ru
```

Должен вернуть `200 OK` без ошибок SSL.

---

## Примеры готовых URL для Bukza

После настройки поддомена используйте в Bukza:

### Для новой записи:
```
https://bot.mycompany.ru/webhook/bukza?message=newrega&phone=%2B7%20%28{phone}%29
```

### Для отмены:
```
https://bot.mycompany.ru/webhook/bukza?message=cancel&phone=%2B7%20%28{phone}%29
```

---

## Частые проблемы

### 1. Поддомен не работает через 30 минут

**Решение:**
- Проверьте правильность IP-адреса
- Убедитесь, что TTL не слишком большой
- Очистите DNS кэш на компьютере:
  ```bash
  # Windows
  ipconfig /flushdns
  
  # Linux/Mac
  sudo systemd-resolve --flush-caches
  ```

### 2. SSL-сертификат не выдается

**Решение:**
- Убедитесь, что DNS уже распространился
- Проверьте, что порт 80 открыт на сервере
- Попробуйте вручную:
  ```bash
  sudo certbot certonly --standalone -d bot.mycompany.ru
  ```

### 3. "DNS_PROBE_FINISHED_NXDOMAIN"

**Решение:**
- DNS еще не распространился, подождите
- Проверьте правильность имени поддомена
- Убедитесь, что запись сохранена в панели управления

---

## Альтернатива: Без поддомена

Если не хотите создавать поддомен, можно использовать:

### 1. Основной домен с путем:
```
https://mycompany.ru/telegram-bot/webhook/bukza
```

Но нужно настроить Nginx, чтобы перенаправлял на порт бота.

### 2. Бесплатный домен от хостинга:
- Railway: `your-bot.up.railway.app`
- Heroku: `your-bot.herokuapp.com`
- Render: `your-bot.onrender.com`

---

## Итоговый чеклист

- [ ] Определились с именем поддомена (`bot`, `telegram`, `api`)
- [ ] Узнали IP сервера (или CNAME для Railway/Heroku)
- [ ] Зашли в панель управления доменом
- [ ] Добавили A-запись (или CNAME)
- [ ] Подождали 5-30 минут
- [ ] Проверили через `ping` или `nslookup`
- [ ] Настроили Nginx на сервере
- [ ] Получили SSL-сертификат через certbot
- [ ] Проверили `https://bot.mycompany.ru`
- [ ] Указали URL в настройках Bukza
- [ ] Протестировали создание записи

---

## Нужна помощь?

Если у вас возникли проблемы:

1. Проверьте, где куплен ваш домен
2. Найдите раздел "DNS" или "DNS-записи" в панели управления
3. Если не можете найти - напишите в поддержку хостинга
4. Укажите, что хотите создать поддомен для API

Большинство хостингов имеют подробные инструкции в базе знаний.
